/** @type {import('next').NextConfig} */
const nextConfig = {
    // Mantemos apenas a configuração de Typescript para garantir
    typescript: {
        ignoreBuildErrors: true,
    },
};

export default nextConfig;